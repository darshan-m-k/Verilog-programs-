module fulladder(input a,b,c,output sum,carry);
task fa(input a,b,c,output sum,carry)
begin
sum=a^b^c;
carry=(a&b)|(a&c)|(b&c);
end
endtask
initial begin
fa(a,b,c,sum,carry);
end
endmodule
 